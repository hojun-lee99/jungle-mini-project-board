$(document).ready(function() {
    
    // 로그인 처리
    $('[type="submit"]').click(function(e) {

        //e.preventDefault();

        const id = $('#id').val();
        const pw = $('#password').val();

        // $.ajax를 통해 /api/auth/login 호출
        


    });

    // 회원가입 처리
    $('#register').click(function(e) {

    });
});